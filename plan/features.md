- add currency feature to select different currencies, it should be one global and individual transactions should can overide the global currency. Put the default one to be the local currency where the user is connceted from. but once set by the user, dont override 

- when a user put a transaction in a category, prompt the user if they want to apply the same categorizaiton on all the payees 

- detect feature to detect duplicates if user mistakingly upload csv with duplicated transaction 

- add subscription category so the user can easily see how much they pay for subscriptions

- add multiselect for tagging / categorizing. 

- add higherchical grouping. for instance, food can have 2 sub groups like restaurant, fast food, etc... or a subscription category can have sub-category of movies, transportation, etc... 

- the user can set a saving goal, per period (week, month, year), default is year. the solution should auto-detect incoming transaction as savings, but user can manually remove or add some. 